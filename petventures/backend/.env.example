# Google Street View Static API key (used in a later slice — stubbed for now).
STREETVIEW_API_KEY=

# Optional: URL of a running ComfyUI instance (generation is stubbed for now).
COMFYUI_URL=http://localhost:8188
